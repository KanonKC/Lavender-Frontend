import React from 'react'

const VerticalMultiStepSection = ({
    children
}: {
    children: React.ReactNode
}) => {
  return (
    <div>
        {children}
    </div>
  )
}

export default VerticalMultiStepSection